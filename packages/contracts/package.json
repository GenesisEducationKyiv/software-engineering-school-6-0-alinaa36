{
  "name": "@grn/contracts",
  "version": "1.0.0",
  "description": "Shared inter-service messaging contract (email queue schema + queue names)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type:check": "tsc --noEmit",
    "prepare": "tsc",
    "proto:lint": "buf lint",
    "proto:generate": "buf generate"
  },
  "license": "ISC",
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.1",
    "@grpc/grpc-js": "^1.14.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.71.0",
    "ts-proto": "^2.12.0",
    "typescript": "^6.0.2"
  }
}
